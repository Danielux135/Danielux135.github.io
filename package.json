{
  "name": "danielux135-portfolio",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/ensure-static-pages.mjs",
    "preview": "vite preview",
    "check": "npm run build",
    "render:showcase": "node scripts/render-showcase-60fps.mjs",
    "preview:video": "node scripts/showcase/recordShowcase.mjs"
  },
  "devDependencies": {
    "playwright": "^1.61.0",
    "vite": "^8.0.0"
  }
}
